# Server-only. Not required during Phase 0.
# DATABASE_URL=postgresql://user:password@localhost:5432/glebia

# Browser-exposed variables must use the NEXT_PUBLIC_ prefix and contain no secrets.
# NEXT_PUBLIC_APP_ENV=development
